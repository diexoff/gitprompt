# Руководство по вкладу в GitPrompt

Спасибо за интерес к проекту GitPrompt! Мы приветствуем вклад от сообщества и ценим ваше участие в развитии библиотеки.

## Содержание

1. [Как внести вклад](#как-внести-вклад)
2. [Настройка среды разработки](#настройка-среды-разработки)
3. [Процесс разработки](#процесс-разработки)
4. [Стандарты кода](#стандарты-кода)
5. [Тестирование](#тестирование)
6. [Документация](#документация)
7. [Создание Pull Request](#создание-pull-request)
8. [Сообщение об ошибках](#сообщение-об-ошибках)
9. [Предложение новых функций](#предложение-новых-функций)

## Как внести вклад

### Типы вкладов

Мы приветствуем различные типы вкладов:

- 🐛 **Исправление ошибок** - исправление найденных багов
- ✨ **Новые функции** - добавление новых возможностей
- 📚 **Документация** - улучшение документации
- 🧪 **Тесты** - добавление или улучшение тестов
- 🔧 **Рефакторинг** - улучшение существующего кода
- 🌍 **Переводы** - перевод документации на другие языки
- 📊 **Производительность** - оптимизация производительности

### Процесс вклада

1. **Fork** репозитория
2. **Создайте ветку** для вашего вклада
3. **Внесите изменения** следуя стандартам проекта
4. **Добавьте тесты** для новых функций
5. **Обновите документацию** при необходимости
6. **Создайте Pull Request**

## Настройка среды разработки

### Требования

- Python 3.9+
- Git
- Poetry (рекомендуется) или pip
- Pre-commit hooks

### Установка

```bash
# Клонируйте ваш fork
git clone https://github.com/yourusername/gitprompt.git
cd gitprompt

# Установите зависимости для разработки
pip install -e ".[dev]"

# Или используйте Poetry
poetry install --with dev

# Установите pre-commit hooks
pre-commit install
```

### Структура проекта

```
gitprompt/
├── src/gitprompt/          # Исходный код библиотеки
├── tests/                  # Тесты
├── examples/               # Примеры использования
├── docs/                   # Документация
├── scripts/                # Скрипты для разработки
├── .github/                # GitHub Actions и шаблоны
├── pyproject.toml          # Конфигурация проекта
├── README.md               # Основная документация
└── CONTRIBUTING.md         # Это руководство
```

## Процесс разработки

### Создание ветки

```bash
# Создайте ветку для вашего вклада
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
# или
git checkout -b docs/your-documentation-update
```

### Соглашения по именованию веток

- `feature/` - новые функции
- `fix/` - исправления ошибок
- `docs/` - обновления документации
- `test/` - добавление тестов
- `refactor/` - рефакторинг
- `perf/` - оптимизация производительности

### Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# Новые функции
git commit -m "feat: add support for new vector database"

# Исправления ошибок
git commit -m "fix: resolve memory leak in embedding service"

# Документация
git commit -m "docs: update API reference for new methods"

# Тесты
git commit -m "test: add integration tests for deployment"

# Рефакторинг
git commit -m "refactor: simplify configuration loading"

# Производительность
git commit -m "perf: optimize batch processing for large files"
```

## Стандарты кода

### Python

Мы следуем стандартам PEP 8 и используем следующие инструменты:

```bash
# Форматирование кода
black src/ tests/

# Проверка стиля
flake8 src/ tests/

# Сортировка импортов
isort src/ tests/

# Проверка типов
mypy src/
```

### Конфигурация инструментов

```toml
# pyproject.toml
[tool.black]
line-length = 88
target-version = ['py39']

[tool.isort]
profile = "black"
multi_line_output = 3

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
```

### Документация кода

Используйте docstrings в стиле Google:

```python
def index_repository(self, path: str, branch: Optional[str] = None) -> Dict[str, Any]:
    """Индексирует репозиторий.
    
    Args:
        path: Путь к репозиторию для индексации
        branch: Ветка для индексации (по умолчанию текущая)
        
    Returns:
        Словарь с результатами индексации:
        - total_files: Количество проиндексированных файлов
        - total_chunks: Количество созданных чанков
        - total_embeddings: Количество созданных эмбеддингов
        
    Raises:
        GitParserError: Если репозиторий не может быть проанализирован
        VectorDatabaseError: Если произошла ошибка при сохранении в БД
        
    Example:
        >>> indexer = GitIndexer(config)
        >>> result = await indexer.index_repository("/path/to/repo")
        >>> print(f"Индексировано {result['total_files']} файлов")
    """
    # Реализация...
```

## Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=src/gitprompt --cov-report=html

# Конкретные тесты
pytest tests/test_core.py

# Интеграционные тесты
pytest -m integration

# Тесты производительности
pytest -m slow
```

### Написание тестов

```python
import pytest
from unittest.mock import Mock, AsyncMock
from gitprompt import GitIndexer, Config

class TestGitIndexer:
    """Тесты для класса GitIndexer."""
    
    @pytest.fixture
    def config(self):
        """Фикстура конфигурации."""
        return Config(
            vector_db=VectorDBConfig(type=VectorDBType.CHROMA),
            llm=LLMConfig(provider=LLMProvider.OPENAI, api_key="test")
        )
    
    @pytest.fixture
    def indexer(self, config):
        """Фикстура индексера."""
        return GitIndexer(config)
    
    @pytest.mark.asyncio
    async def test_index_repository_success(self, indexer):
        """Тест успешной индексации репозитория."""
        # Arrange
        repo_path = "/path/to/test/repo"
        expected_result = {
            "total_files": 10,
            "total_chunks": 25,
            "total_embeddings": 25
        }
        
        # Act
        result = await indexer.index_repository(repo_path)
        
        # Assert
        assert result["total_files"] > 0
        assert "total_chunks" in result
        assert "total_embeddings" in result
    
    @pytest.mark.asyncio
    async def test_index_repository_invalid_path(self, indexer):
        """Тест индексации несуществующего репозитория."""
        # Arrange
        invalid_path = "/path/to/nonexistent/repo"
        
        # Act & Assert
        with pytest.raises(GitParserError):
            await indexer.index_repository(invalid_path)
```

### Типы тестов

- **Unit тесты** - тестирование отдельных функций и классов
- **Integration тесты** - тестирование взаимодействия компонентов
- **Performance тесты** - тестирование производительности
- **CLI тесты** - тестирование командной строки

## Документация

### Обновление документации

При добавлении новых функций обязательно обновите:

1. **Docstrings** в коде
2. **API Reference** в `docs/API_REFERENCE.md`
3. **Примеры** в `docs/EXAMPLES.md`
4. **README** при необходимости

### Стиль документации

- Используйте четкий и понятный язык
- Добавляйте примеры кода
- Объясняйте сложные концепции
- Используйте эмодзи для улучшения читаемости

### Проверка документации

```bash
# Проверка ссылок в документации
markdown-link-check docs/*.md

# Проверка орфографии
aspell check docs/README.md
```

## Создание Pull Request

### Перед созданием PR

1. **Убедитесь, что тесты проходят**:
   ```bash
   pytest
   ```

2. **Проверьте качество кода**:
   ```bash
   black --check src/ tests/
   flake8 src/ tests/
   mypy src/
   ```

3. **Обновите документацию** при необходимости

4. **Добавьте тесты** для новых функций

### Создание PR

1. **Push** ваши изменения:
   ```bash
   git push origin feature/your-feature-name
   ```

2. **Создайте Pull Request** на GitHub

3. **Заполните шаблон PR**:
   - Описание изменений
   - Связанные issues
   - Тип изменений
   - Чек-лист

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Исправление ошибки
- [ ] Новая функция
- [ ] Обновление документации
- [ ] Рефакторинг
- [ ] Тесты

## Связанные Issues
Closes #123

## Чек-лист
- [ ] Код следует стандартам проекта
- [ ] Добавлены тесты для новых функций
- [ ] Обновлена документация
- [ ] Все тесты проходят
- [ ] Проверка типов проходит

## Дополнительные заметки
Любая дополнительная информация
```

### Процесс ревью

1. **Автоматические проверки** - CI/CD pipeline
2. **Ревью кода** - проверка другими разработчиками
3. **Тестирование** - проверка функциональности
4. **Мерж** - после одобрения

## Сообщение об ошибках

### Создание Issue

Используйте шаблон для сообщения об ошибках:

```markdown
## Описание ошибки
Четкое описание проблемы

## Шаги для воспроизведения
1. Перейти к '...'
2. Нажать на '...'
3. Прокрутить до '...'
4. Увидеть ошибку

## Ожидаемое поведение
Что должно было произойти

## Фактическое поведение
Что произошло на самом деле

## Скриншоты
Если применимо, добавьте скриншоты

## Окружение
- OS: [e.g. Ubuntu 20.04]
- Python: [e.g. 3.9.7]
- GitPrompt: [e.g. 1.0.0]

## Дополнительная информация
Любая дополнительная информация об ошибке
```

### Информация для отладки

```python
# Добавьте эту информацию в issue
import gitprompt
import sys

print(f"GitPrompt version: {gitprompt.__version__}")
print(f"Python version: {sys.version}")
print(f"Platform: {sys.platform}")

# Логи ошибок
import logging
logging.basicConfig(level=logging.DEBUG)
# Ваш код, который вызывает ошибку
```

## Предложение новых функций

### Создание Feature Request

```markdown
## Описание функции
Четкое описание желаемой функции

## Проблема
Какую проблему решает эта функция?

## Предлагаемое решение
Как вы видите реализацию?

## Альтернативы
Какие альтернативные решения вы рассматривали?

## Дополнительная информация
Любая дополнительная информация
```

### Процесс одобрения

1. **Обсуждение** - обсуждение с сообществом
2. **Планирование** - планирование реализации
3. **Разработка** - создание PR
4. **Тестирование** - тестирование функции
5. **Релиз** - включение в релиз

## Стиль общения

### В Issues и PR

- Будьте вежливы и уважительны
- Используйте четкий и понятный язык
- Предоставляйте достаточно контекста
- Благодарите за помощь

### В коде

- Используйте понятные имена переменных
- Добавляйте комментарии для сложной логики
- Следуйте принципам SOLID
- Пишите чистый и читаемый код

## Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш вклад будет лицензирован под MIT License.

## Получение помощи

Если у вас есть вопросы:

- Создайте **Issue** с вопросом
- Присоединитесь к **Discussions**
- Обратитесь к **документации**
- Изучите **существующие Issues**

## Благодарности

Спасибо всем, кто вносит вклад в развитие GitPrompt! Ваши усилия делают библиотеку лучше для всего сообщества.

---

**Спасибо за ваш вклад в GitPrompt! 🚀**
