# Чек-лист релиза GitPrompt

Этот документ содержит чек-лист для подготовки и выпуска новых версий GitPrompt.

## Содержание

1. [Подготовка к релизу](#подготовка-к-релизу)
2. [Тестирование](#тестирование)
3. [Документация](#документация)
4. [Создание релиза](#создание-релиза)
5. [Пост-релиз](#пост-релиз)

## Подготовка к релизу

### ✅ Проверка кода

- [ ] Все изменения закоммичены и запушены
- [ ] Код проходит все линтеры (black, flake8, mypy)
- [ ] Pre-commit hooks настроены и работают
- [ ] Нет TODO или FIXME комментариев в коде
- [ ] Все временные файлы и отладочный код удалены

### ✅ Обновление версии

- [ ] Обновлена версия в `src/gitprompt/version.py`
- [ ] Обновлена версия в `pyproject.toml`
- [ ] Обновлена версия в `setup.py`
- [ ] Обновлена версия в `README.md` (если необходимо)
- [ ] Создан тег Git для новой версии

```bash
# Обновление версии
git tag -a v1.1.0 -m "Release version 1.1.0"
git push origin v1.1.0
```

### ✅ Проверка зависимостей

- [ ] Все зависимости обновлены до последних стабильных версий
- [ ] Проверены уязвимости в зависимостях
- [ ] Обновлен `requirements.txt`
- [ ] Обновлен `pyproject.toml` с новыми зависимостями

```bash
# Проверка уязвимостей
pip install safety
safety check

# Обновление зависимостей
pip install --upgrade -r requirements.txt
```

## Тестирование

### ✅ Unit тесты

- [ ] Все unit тесты проходят
- [ ] Покрытие кода не менее 80%
- [ ] Добавлены тесты для новых функций
- [ ] Обновлены тесты для измененных функций

```bash
# Запуск unit тестов
pytest tests/ -v --cov=src/gitprompt --cov-report=html

# Проверка покрытия
pytest --cov=src/gitprompt --cov-report=term-missing
```

### ✅ Интеграционные тесты

- [ ] Интеграционные тесты проходят
- [ ] Тесты с реальными API провайдерами работают
- [ ] Тесты с векторными базами данных проходят
- [ ] Тесты CLI интерфейса работают

```bash
# Запуск интеграционных тестов
pytest tests/ -m integration -v

# Тесты с реальными API (требуют API ключи)
pytest tests/ -m "integration and not mock" -v
```

### ✅ Тесты производительности

- [ ] Тесты производительности проходят
- [ ] Нет регрессий в производительности
- [ ] Память используется эффективно
- [ ] Время выполнения в пределах нормы

```bash
# Запуск тестов производительности
pytest tests/ -m slow -v

# Профилирование памяти
python -m memory_profiler examples/performance_test.py
```

### ✅ Тесты совместимости

- [ ] Тесты на Python 3.9, 3.10, 3.11 проходят
- [ ] Тесты на разных операционных системах проходят
- [ ] Тесты с разными версиями зависимостей проходят
- [ ] Обратная совместимость сохранена

```bash
# Тесты на разных версиях Python
tox

# Тесты на разных ОС (если доступно)
# GitHub Actions автоматически тестирует на разных ОС
```

### ✅ Ручное тестирование

- [ ] Базовые примеры работают
- [ ] CLI команды работают корректно
- [ ] Документация соответствует реальному поведению
- [ ] Примеры в документации работают

```bash
# Тестирование CLI
gitprompt --help
gitprompt config --output test_config.json
gitprompt index /path/to/test/repo

# Тестирование примеров
python examples/basic_usage.py
python examples/advanced_usage.py
```

## Документация

### ✅ Обновление документации

- [ ] README.md обновлен с новыми функциями
- [ ] API Reference обновлен
- [ ] Примеры использования обновлены
- [ ] Конфигурация документирована
- [ ] Руководство по развертыванию актуально

### ✅ Проверка документации

- [ ] Все ссылки в документации работают
- [ ] Примеры кода синтаксически корректны
- [ ] Документация соответствует коду
- [ ] Нет орфографических ошибок
- [ ] Форматирование Markdown корректно

```bash
# Проверка ссылок
markdown-link-check docs/*.md

# Проверка орфографии
aspell check docs/README.md

# Проверка синтаксиса Python в примерах
python -m py_compile examples/*.py
```

### ✅ Changelog

- [ ] CHANGELOG.md обновлен
- [ ] Все изменения задокументированы
- [ ] Breaking changes выделены
- [ ] Новые функции описаны
- [ ] Исправления ошибок перечислены

```markdown
# Changelog

## [1.1.0] - 2024-01-15

### Added
- Поддержка новой векторной базы данных Qdrant
- CLI команда для мониторинга изменений
- Автоматическое обновление эмбеддингов при изменениях

### Changed
- Улучшена производительность индексации больших репозиториев
- Обновлен API для работы с ветками

### Fixed
- Исправлена утечка памяти при длительной работе
- Исправлена ошибка с кодировкой файлов

### Security
- Улучшена безопасность хранения API ключей
```

## Создание релиза

### ✅ Сборка пакета

- [ ] Пакет собирается без ошибок
- [ ] Все файлы включены в пакет
- [ ] Метаданные корректны
- [ ] Зависимости правильно указаны

```bash
# Очистка предыдущих сборок
rm -rf build/ dist/ *.egg-info/

# Сборка пакета
python -m build

# Проверка пакета
twine check dist/*
```

### ✅ Тестирование пакета

- [ ] Пакет устанавливается без ошибок
- [ ] Все функции работают после установки
- [ ] Импорты работают корректно
- [ ] CLI доступен после установки

```bash
# Установка пакета в виртуальное окружение
python -m venv test_env
source test_env/bin/activate
pip install dist/gitprompt-1.1.0-py3-none-any.whl

# Тестирование установленного пакета
python -c "import gitprompt; print(gitprompt.__version__)"
gitprompt --help
```

### ✅ Загрузка в PyPI

- [ ] Пакет загружен в PyPI
- [ ] Пакет доступен для установки
- [ ] Метаданные отображаются корректно
- [ ] Документация на PyPI актуальна

```bash
# Загрузка в PyPI (требует токен)
twine upload dist/*

# Проверка загрузки
pip install gitprompt==1.1.0
```

### ✅ GitHub Release

- [ ] Создан релиз на GitHub
- [ ] Release notes написаны
- [ ] Теги созданы
- [ ] Артефакты прикреплены

```bash
# Создание релиза через GitHub CLI
gh release create v1.1.0 \
  --title "GitPrompt v1.1.0" \
  --notes-file CHANGELOG.md \
  dist/gitprompt-1.1.0-py3-none-any.whl
```

## Пост-релиз

### ✅ Уведомления

- [ ] Уведомление отправлено в рассылку
- [ ] Пост в блоге опубликован
- [ ] Социальные сети обновлены
- [ ] Документация обновлена

### ✅ Мониторинг

- [ ] Мониторинг установок пакета
- [ ] Отслеживание ошибок и issues
- [ ] Мониторинг производительности
- [ ] Сбор обратной связи

### ✅ Подготовка к следующему релизу

- [ ] Создана ветка для следующего релиза
- [ ] Планирование следующих функций
- [ ] Обновление roadmap
- [ ] Анализ метрик использования

## Автоматизация

### GitHub Actions

```yaml
# .github/workflows/release.yml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install build twine
        pip install -r requirements.txt
    
    - name: Run tests
      run: pytest tests/ -v
    
    - name: Build package
      run: python -m build
    
    - name: Check package
      run: twine check dist/*
    
    - name: Upload to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: twine upload dist/*
```

### Скрипт релиза

```bash
#!/bin/bash
# scripts/release.sh

set -e

VERSION=$1
if [ -z "$VERSION" ]; then
    echo "Usage: $0 <version>"
    exit 1
fi

echo "Releasing version $VERSION..."

# Обновление версии
echo "Updating version..."
sed -i "s/__version__ = .*/__version__ = \"$VERSION\"/" src/gitprompt/version.py
sed -i "s/version = .*/version = \"$VERSION\"/" pyproject.toml

# Запуск тестов
echo "Running tests..."
pytest tests/ -v

# Сборка пакета
echo "Building package..."
python -m build

# Проверка пакета
echo "Checking package..."
twine check dist/*

# Создание тега
echo "Creating tag..."
git add .
git commit -m "Release version $VERSION"
git tag -a "v$VERSION" -m "Release version $VERSION"
git push origin "v$VERSION"

# Загрузка в PyPI
echo "Uploading to PyPI..."
twine upload dist/*

echo "Release $VERSION completed!"
```

## Контрольный список

### Перед релизом

- [ ] Код готов к релизу
- [ ] Тесты проходят
- [ ] Документация обновлена
- [ ] Версия обновлена
- [ ] Changelog готов

### Во время релиза

- [ ] Пакет собран
- [ ] Пакет протестирован
- [ ] Пакет загружен в PyPI
- [ ] GitHub релиз создан
- [ ] Теги созданы

### После релиза

- [ ] Уведомления отправлены
- [ ] Мониторинг настроен
- [ ] Обратная связь собрана
- [ ] Планы на следующий релиз

---

**Помните: Качественный релиз требует времени и внимания к деталям. Не торопитесь и проверяйте каждый пункт! 🚀**
